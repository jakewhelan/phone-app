import { IterateOverObjectPipe } from './iterate-over-object.pipe';

describe('IterateOverObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new IterateOverObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
